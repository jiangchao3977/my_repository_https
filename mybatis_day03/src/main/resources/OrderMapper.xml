<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com03.OrderMapper">
    <resultMap id="Order" type="com03.Order">
        <!--column="ordertime"是数据库中的字段 property="ordertime"是实体类的属性-->
        <id     column="id"        property="id" />
        <result column="ordertime" property="ordertime"/>
        <result column="total"     property="total"/>
      <!--<result column="id"        property="user.id"/>
          <result column="username"  property="user.username"/>
          <result column="password"  property="user.password"/>
          <result column="birthday"  property="user.birthday"/>-->
        <!--javaType: 当前实体(order)中的属性类型(User)
            property: 当前实体(order)中的属性名称(private User user)-->
            <association javaType="user" property="user">
            <id column="uid" property="id"/>
            <result column="username" property="username"/>
            <result column="password" property="password"/>
            <result column="birthday" property="birthday"/>
        </association>
    </resultMap>
    <select id="fiandAll" resultMap="Order">
        SELECT * FROM orders,mybatis01 WHERE orders.uid=mybatis01.id;
          /*SELECT *,o.id oid FROM USER u,orders o WHERE u.id=o.uid*/
    </select>

   <!-- <resultMap id="orderMap" type="order">
        &lt;!&ndash;手动指定字段与实体属性的映射关系
            column: 数据表的字段名称
            property：实体的属性名称
        &ndash;&gt;
        <id column="oid" property="id"></id>
        <result column="ordertime" property="ordertime"></result>
        <result column="total" property="total"></result>
        &lt;!&ndash;<result column="uid" property="user.id"></result>
        <result column="username" property="user.username"></result>
        <result column="password" property="user.password"></result>
        <result column="birthday" property="user.birthday"></result>&ndash;&gt;

        &lt;!&ndash;
            property: 当前实体(order)中的属性名称(private User user)
            javaType: 当前实体(order)中的属性的类型(User)
        &ndash;&gt;
        <association property="user" javaType="user">
            <id column="uid" property="id"></id>
            <result column="username" property="username"></result>
            <result column="password" property="password"></result>
            <result column="birthday" property="birthday"></result>
        </association>
    </resultMap>-->
</mapper>
